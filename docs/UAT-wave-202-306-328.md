# UAT wave: CMPT 202, 306, 328

Commands assume a configured `.env` with Canvas API access and `uv sync` run
under `portfolio/canvas-archive`.

## CMPT 202 (`canvas_only`, canvas_id 3462700)

| Check | Notes |
| :-- | :-- |
| Profile uses explicit hosts only | Host allowlist; `developer_mode` off. |
| Assignment groups | Homework-only follow (`only_assignment_groups`). |
| Milestones | Canvas-only; see profile comments. |
| Extract | `uv run canvas-archive run 3462700` |
| Verify | Pipeline prints `verify-assignments-md: OK`. |

Profile: [cmpt-202-intro-to-data-structures.yaml](../profiles/cmpt-202-intro-to-data-structures.yaml).

**Last UAT (2026-05-14):** `uv run canvas-archive run 3462700` completed;
`verify-assignments-md: OK`; 53 assignments, 3 follow_description_links merges.

## CMPT 306 (`external_site`, canvas_id 3506954)

| Check | Notes |
| :-- | :-- |
| Regex ladder | Extend patterns if Canvas titles drift. |
| Mirror | `wget` uses `external_site.base_url`; log shows `course root:`. |
| Extract | `uv run canvas-archive run 3506954` |

Profile: [cmpt-306-algorithms.yaml](../profiles/cmpt-306-algorithms.yaml).

**Last UAT (2026-05-14):** Extract OK; 19 assignments enriched from external
site, 13 lab starters copied, 20 handouts HTML and 8 PDFs;
`verify-assignments-md: OK`.

## CMPT 328 (`external_site`, canvas_id 3596470)

| Check | Notes |
| :-- | :-- |
| JS-render risk | See [CMPT328-external-site-and-js-render.md](CMPT328-external-site-and-js-render.md). |
| Extract | `uv run canvas-archive run 3596470` |

**Last UAT (2026-05-14):** `wget` exited 8; mirror reported **0 files** and
extractor could not find `course root` (no enrich). Canvas-only pass still
produced 33 `.md` files; verify OK. Follow the JS-render doc or fix `base_url`
or server access before expecting mirrored homework bodies.

## Post-UAT lockdown

- Set `developer_mode: false` (or omit) on any course profile before treating
  the archive as production-complete.
- Never set `course_home_external_starters_allow_any_https_host: true` unless
  `developer_mode: true`; the pipeline calls `validate_profile_security_flags`
  and will exit if that pair is inconsistent.
